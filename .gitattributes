# Preserve the exact CSV fixture bytes, including BOM and CRLF, across clones.
data/reference-cases/**/*.csv -text whitespace=cr-at-eol

# Two trailing spaces are intentional Markdown hard line breaks.
*.md whitespace=-blank-at-eol
